{
    if true do { }
    if ok do { }
    if flip() do {
        return $coin
    }
    if list.isEmpty() AND value ≠ none do {
        list.addValue(value)
    }
    if NOT successful do {
        throw [
            $type: $bad
            $kind: "worse"
        ]
    }
}
